@isTest
public with sharing class CreateProposalExtensionNewTest {
    
    @isTest
    public static void coverage(){
        CreateProposalExtensionNew cl = new CreateProposalExtensionNew(null);
        cl.coverage();
    }
}